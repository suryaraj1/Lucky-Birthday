import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <div className='btn-wrapper'>
            <button>Check Number</button>
        </div>
    )
}

export default Button;